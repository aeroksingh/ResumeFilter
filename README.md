
# ResumeFilter 🎯

An **AI-powered interview preparation platform** built with the MERN stack. Upload your resume and a job description to instantly get a job match score, tailored interview questions, skill gap analysis, a day-wise preparation plan, and an ATS-optimized resume PDF — all generated by Google Gemini AI.

---

## Features

- 📄 **Resume Parsing** — Upload your resume as a PDF; the app extracts and analyzes the content
- 🤖 **AI Interview Report** — Generates technical & behavioral questions, skill gaps, and a day-wise prep plan using Google Gemini AI
- 📊 **Job Match Score** — Scores how well your profile matches the job description (0–100)
- 📝 **ATS Resume PDF** — Generates a tailored, ATS-friendly resume PDF using Puppeteer
- 🔐 **JWT Authentication** — Secure login/register with token blacklisting on logout

---

## Tech Stack

| Layer    | Technology                                      |
|----------|-------------------------------------------------|
| Frontend | React 19, React Router 7, Axios, SCSS, Vite     |
| Backend  | Node.js, Express.js 5, Mongoose                 |
| Database | MongoDB                                         |
| AI       | Google Gemini AI (`@google/genai`)              |
| Auth     | JWT, bcryptjs, cookie-parser                    |
| PDF      | Puppeteer, pdf-parse, Multer                    |

---

## Project Structure

```
ResumeFilter/
├── Backend/
│   ├── server.js
│   └── src/
│       ├── app.js
│       ├── config/        # MongoDB connection
│       ├── controllers/   # auth, interview logic
│       ├── middlewares/   # auth guard, file upload
│       ├── models/        # User, InterviewReport, Blacklist
│       ├── routes/        # /api/auth, /api/interview
│       └── services/      # Gemini AI, Puppeteer PDF
└── Frontend/
    └── src/
        ├── features/
        │   ├── auth/      # Login, Register, Protected routes
        │   └── interview/ # Report generation, display
        └── App.jsx
```

---

## Prerequisites

Make sure you have the following installed:

- [Node.js](https://nodejs.org/) v18+
- [npm](https://www.npmjs.com/) v9+
- [MongoDB](https://www.mongodb.com/) (local or [MongoDB Atlas](https://www.mongodb.com/cloud/atlas))
- A [Google Gemini API Key](https://aistudio.google.com/app/apikey)

---

## Installation & Setup

### 1. Clone the repository

```bash
git clone https://github.com/aeroksingh/ResumeFilter.git
cd ResumeFilter
```

### 2. Backend Setup

```bash
cd Backend
npm install
```

Create a `.env` file in the `Backend/` directory:

```env
PORT=3000
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret_key
GOOGLE_GENAI_API_KEY=your_google_gemini_api_key
```

Start the backend server:

```bash
npm run dev
```

The backend runs on `http://localhost:3000`

### 3. Frontend Setup

```bash
cd Frontend
npm install
npm run dev
```

The frontend runs on `http://localhost:5173`

---

## API Reference

### Auth Routes — `/api/auth`

| Method | Endpoint    | Access  | Description                  |
|--------|-------------|---------|------------------------------|
| POST   | `/register` | Public  | Register a new user          |
| POST   | `/login`    | Public  | Login and receive JWT cookie |
| GET    | `/logout`   | Public  | Logout and blacklist token   |
| GET    | `/get-me`   | Private | Get current logged-in user   |

### Interview Routes — `/api/interview`

| Method | Endpoint                         | Access  | Description                                      |
|--------|----------------------------------|---------|--------------------------------------------------|
| POST   | `/`                              | Private | Generate interview report (upload resume PDF)    |
| GET    | `/`                              | Private | Get all reports for logged-in user               |
| GET    | `/report/:interviewId`           | Private | Get a specific report by ID                      |
| POST   | `/resume/pdf/:interviewReportId` | Private | Generate tailored resume PDF                     |

---

## Environment Variables

| Variable               | Description                           |
|------------------------|---------------------------------------|
| `PORT`                 | Backend server port (default: 3000)   |
| `MONGO_URI`            | MongoDB connection string             |
| `JWT_SECRET`           | Secret key for signing JWT tokens     |
| `GOOGLE_GENAI_API_KEY` | Google Gemini AI API key              |

---

## Usage

1. Register or login at `http://localhost:5173`
2. Upload your resume (PDF) and paste the job description
3. Add a short self-description about yourself
4. Submit to generate your AI interview report
5. View your match score, questions, skill gaps, and prep plan
6. Download your ATS-optimized resume PDF

---

## Author

**Ashutosh Singh**
- GitHub: [@aeroksingh](https://github.com/aeroksingh)
- LinkedIn: [aeroksingh0112](https://www.linkedin.com/in/aeroksingh0112/)
```
